#!/usr/bin/env bash
# Perception CLI - Quick launcher for the TUI
# Usage: ./perception [dashboard|feeds|test|status]

cd "$(dirname "$0")"

# Activate venv if available
if [ -f ".venv/bin/activate" ]; then
    source .venv/bin/activate
fi

python3 perception_tui.py "$@"
